import { FilterBreedsDogs } from '../components/FilterBreedsDogs'
import { render } from '@testing-library/react'

describe('FilterBreedsDogs', () => {
  it('shoul render components', () => {
    render(<FilterBreedsDogs />)
  })
})
